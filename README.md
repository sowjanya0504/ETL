# ETL
# This codes extracts sales data from an SQLite database to analyze total quantities of items bought by customers aged 18-35. 
# It provides two solutions: one using SQL and the other using Python with Pandas. The results are exported to CSV files with a semicolon delimiter.
